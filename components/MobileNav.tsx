'use client';import Link from 'next/link';import {useState} from 'react';export function MobileNav({links}:{links:string[][]}){const [open,setOpen]=useState(false);return <div className="mobileNav"><button className="menuButton" aria-label="Open menu" aria-expanded={open} onClick={()=>setOpen(!open)}>☰</button>{open&&<div className="mobileMenu">{links.map(([label,href])=><Link href={href} key={href} onClick={()=>setOpen(false)}>{label}</Link>)}<Link className="headerCta" href="/kontak/" onClick={()=>setOpen(false)}>Konsultasikan Proyek ↗</Link></div>}</div>}