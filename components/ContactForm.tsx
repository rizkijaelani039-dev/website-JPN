'use client';
import {useState} from 'react';
export function ContactForm(){const [sent,setSent]=useState(false);function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const subject=encodeURIComponent(`Inquiry JPN - ${f.get('name')}`);const body=encodeURIComponent(`Nama: ${f.get('name')}\nEmail: ${f.get('email')}\nTelepon: ${f.get('phone')}\nLayanan: ${f.get('service')}\nKebutuhan:\n${f.get('message')}`);window.location.href=`mailto:primansntr@gmail.com?subject=${subject}&body=${body}`;setSent(true)}return <form className="contactForm" onSubmit={submit}><label>Nama<input name="name" required /></label><label>Email<input name="email" type="email" required /></label><label>Telepon / WhatsApp<input name="phone" /></label><label>Layanan<select name="service" defaultValue=""><option value="" disabled>Pilih layanan</option><option>Electrical</option><option>Renewable Energy</option><option>Telecom</option><option>EV Charger</option></select></label><label>Kebutuhan proyek<textarea name="message" rows={6} required /></label><button className="btn" type="submit">Kirim Inquiry</button>{sent&&<p className="muted">Draft email sudah disiapkan. Silakan lanjutkan pengiriman dari aplikasi email Anda.</p>}</form>}