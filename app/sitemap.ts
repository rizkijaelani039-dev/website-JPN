import type {MetadataRoute} from 'next';import {services} from '@/data/site';import {articles} from '@/data/articles';export default function sitemap():MetadataRoute.Sitemap{const base='https://rizkijaelani039-dev.github.io/website-JPN';return ['','layanan/','proyek/','tentang/','knowledge/','kontak/'].map(path=>({url:`${base}/${path}`})).concat(services.map(s=>({url:`${base}/layanan/${s.slug}/`})),articles.map(a=>({url:`${base}/knowledge/${a.slug}/`})))}