import {notFound} from 'next/navigation';import type {Metadata} from 'next';import {articles} from '@/data/articles';
export function generateStaticParams(){return articles.map(a=>({slug:a.slug}))}
export function generateMetadata({params}:{params:{slug:string}}):Metadata{const a=articles.find(x=>x.slug===params.slug);return a?{title:a.title,description:a.excerpt}:{title:'Knowledge JPN'}}
export default function Article({params}:{params:{slug:string}}){const a=articles.find(x=>x.slug===params.slug);if(!a)notFound();return <article className="section"><div className="breadcrumb">Home / Knowledge / {a.category}</div><div className="sectionHead"><div className="eyebrow">{a.category}</div><h1>{a.title}</h1><p className="muted">{a.excerpt}</p></div><div style={{maxWidth:780}}>{a.content.map(p=><p className="muted" key={p}>{p}</p>)}</div><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify({'@context':'https://schema.org','@type':'Article','headline':a.title,'description':a.excerpt,'author':{'@type':'Organization','name':'CV. Jaya Prima Nusantara'}})}}/></article>}